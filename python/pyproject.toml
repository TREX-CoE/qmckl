[build-system]
requires = [
    "setuptools>=42",
    "wheel",
]
build-backend = "setuptools.build_meta"

[project]
name = "qmckl"
version = "1.1.0"
description = "Python API of the QMCkl library"
readme = "README.md"
license = "BSD-3-Clause"
requires-python = ">=3.6"
dependencies = ["numpy"]
